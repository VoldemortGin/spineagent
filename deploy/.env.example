# spineagent 部署环境变量样例 —— 拷为 deploy/.env 用（docker compose 自动读取同目录 .env）。
#
# 【诚实声明】spineagent 自身【不读任何 SPINEAGENT_* 旋钮】(其 src/ 对 os.environ 零引用),
# 配置纯走 Python 代码(构造对象、传参、选 provider spec)。因此这里【没有】像 ragspine 那样的
# 一长串 PREFIX_* 应用旋钮 —— 凭空造旋钮就是伪旋钮,family 宪章不允许。
#
# 唯一有效的运行时变量是【真实 LLM provider 的 *_API_KEY】,且仅当你把代码/入口改成显式选用
# 对应真实 provider 时,才由该厂商 SDK 自行从环境读取。默认 demo(examples/quickstart.py)走
# 离线 MockProvider,以下全部留空即可零 key 跑通。
#
# 安装对应 extra 才有真实 SDK:pip install "spineagent[anthropic]" / "[openai]" / "[all]" 等。

# Anthropic（spineagent[anthropic]，AnthropicProvider 选用时由 anthropic SDK 读取）。
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI 及 OpenAI 兼容端点（spineagent[openai]，OpenAICompatProvider 选用时由 openai SDK 读取）。
# OPENAI_API_KEY=sk-...
